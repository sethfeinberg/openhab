import org.openhab.core.library.types.*

rule "Wii w Music State Macro"
when
	Item WiiwMusicStateMacro received command
then
  sendCommand(AV_TV_IP_InputComponent, ON)
  sendCommand(AV_Denon_IP_AudioMusic, ON)
  sendCommand(AV_Denon_IP_MainInputGame, ON)
end
