import org.openhab.core.library.types.*

rule "Wii Macro"
when
	Item WiiPowerMacro received command
then
  sendCommand(AV_TV_IP_InputHDMI4, ON)
  sendCommand(AV_Denon_IP_AudioGame, ON)
  sendCommand(AV_Denon_IP_MainInputCBLSAT, ON)
end