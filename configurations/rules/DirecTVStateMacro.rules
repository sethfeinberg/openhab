import org.openhab.core.library.types.*

rule "DirecTV Macro"
when
	Item DirecTVStateMacro received command
then
  sendCommand(AV_TV_IP_InputHDMI4, ON)
  sendCommand(AV_Denon_IP_AudioMovie, ON)
  sendCommand(AV_Denon_IP_MainInputDVD, ON)
end
